name: Build Android APK

on:
  workflow_dispatch: {}

env:
  QT_VERSION: '6.5.3'
  NDK_VERSION: '27.2.12479018'
  ANDROID_PLATFORM: 'android-33'
  ANDROID_ABI: 'arm64-v8a'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install SDK & NDK packages
        run: |
          yes | sdkmanager --licenses > /dev/null
          sdkmanager "platforms;android-33" "build-tools;33.0.2" "ndk;27.2.12479018"

      - name: Install Qt (Android arm64-v8a)
        id: qt-android
        uses: jurplel/install-qt-action@v3
        with:
          version: ${{ env.QT_VERSION }}
          host: linux
          target: android
          arch: android_arm64_v8a
          modules: qtmultimedia

      - name: Install Qt (host gcc_64)
        id: qt-host
        uses: jurplel/install-qt-action@v3
        with:
          version: ${{ env.QT_VERSION }}
          host: linux
          target: desktop
          arch: gcc_64

      - name: Install vcpkg deps (arm64-android)
        run: |
          export ANDROID_NDK_HOME="$ANDROID_SDK_ROOT/ndk/${{ env.NDK_VERSION }}"
          export ANDROID_NDK_ROOT="$ANDROID_NDK_HOME"
          vcpkg install \
            --triplet arm64-android \
            --x-install-root=${{ github.workspace }}/build-vcpkg-android/vcpkg_installed

      - name: Configure CMake
        run: |
          QT_ANDROID=$GITHUB_WORKSPACE/../Qt/6.5.3/android_arm64_v8a
          QT_HOST=$GITHUB_WORKSPACE/../Qt/6.5.3/gcc_64
          cmake -S . -B build-android-qr-arm64 -G Ninja \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_TOOLCHAIN_FILE=$QT_ANDROID/lib/cmake/Qt6/qt.toolchain.cmake \
            -DCMAKE_PREFIX_PATH=$QT_ANDROID \
            -DQT_HOST_PATH=$QT_HOST \
            -DANDROID_ABI=${{ env.ANDROID_ABI }} \
            -DANDROID_PLATFORM=${{ env.ANDROID_PLATFORM }} \
            -DAIRGAP_QR_DEP_ROOT=${{ github.workspace }}/build-vcpkg-android/vcpkg_installed/arm64-android \
            -DBUILD_TESTING=OFF

      - name: Build APK
        run: cmake --build build-android-qr-arm64 --parallel

      - name: Locate APK
        run: |
          APK=$(find build-android-qr-arm64 -name '*.apk' | head -1)
          echo "Found APK: $APK"
          echo "APK_PATH=$APK" >> $GITHUB_ENV

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: airgap-android-arm64-debug-apk
          path: ${{ env.APK_PATH }}

      - name: Upload to release
        uses: softprops/action-gh-release@v2
        with:
          tag: v0.2.8
          files: ${{ env.APK_PATH }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
